<!DOCTYPE html>
<html {{ site.language_attributes }}>
	<head>
		<meta charset="{{ site.charset }}" />
		{{ function('wp_head') }}
	</head>

	<body class="{{ body_class }}">
		<div id="main-container">
			{% include 'header.twig' %}
			<main>
				{% block body %}

				{% endblock %}
			</main>
			{% include 'footer.twig' %}
		</div>

		{{ function('wp_footer') }}
	</body>
</html>
